import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    background: {
      default: "#101010",
    },
    text: {
      primary: "#ffff",
      secondary: "#000",
    },
    primary: {
      main: "#181F2C",
    },
    secondary: {
      main: "#EFBF3A",
    },
  },
});
