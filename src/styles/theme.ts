import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    background: {
      default: "#222835",
    },
    text: {
      primary: "#ffff",
      secondary: "#000",
    },
    primary: {
      main: "#181F2C",
    },
  },
});
